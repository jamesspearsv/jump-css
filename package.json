{
  "name": "jump-css",
  "version": "0.4.0",
  "type": "module",
  "exports": "./dist/jump.min.css",
  "files": [
    "package.json",
    "./dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "lightningcss --minify --bundle --targets \">= 0.25%\" -o dist/jump.min.css src/jump.css",
    "publish:dry": "pnpm publish --dry-run",
    "publish:dist": "pnpm publish"
  },
  "devDependencies": {
    "lightningcss-cli": "^1.30.1"
  }
}
